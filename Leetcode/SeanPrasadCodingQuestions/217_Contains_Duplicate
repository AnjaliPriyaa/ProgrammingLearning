class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        # newnums = set() //O[n] space and time
        # for i in range(len(nums)):
        #     newnums.add(nums[i])
        # if len(newnums) == len(nums):
        #     return False
        # return True

        # return len(nums) != len(set(nums)) // DO it one Line

        newnums = set()
        for i in nums:
            if i in newnums:
                return True
            newnums.add(i)
        return False

  
